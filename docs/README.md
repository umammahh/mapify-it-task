# MapifyIt Developer Test — Quick Docs

Structure:
- frontend/  (React + Leaflet)
- backend/   (Express APIs: routing, geocoding, enrichment scripts)
- data/      (raw + enriched datasets)
- docs/      (explanations + submission files)

Run locally:
- Frontend: cd frontend && npm run dev
- Backend : cd backend && npm start

Env:
- See .env.example
